{
  "name": "@iotpilot/backend",
  "version": "0.1.0",
  "private": true,
  "description": "IoT Pilot — Express API backend",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx watch src/server.ts",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "db:generate": "prisma generate --schema=./prisma/schema.prisma",
    "db:migrate": "prisma migrate deploy --schema=./prisma/schema.prisma",
    "db:migrate:deploy": "prisma migrate deploy --schema=./prisma/schema.prisma",
    "db:migrate:dev": "prisma migrate dev --schema=./prisma/schema.prisma",
    "db:migrate:resolve": "prisma migrate resolve --applied --schema=./prisma/schema.prisma",
    "db:push": "prisma db push --schema=./prisma/schema.prisma --accept-data-loss"
  },
  "dependencies": {
    "@iotpilot/core": "workspace:*",
    "@paralleldrive/cuid2": "^2.3.1",
    "@prisma/client": "^6.19.0",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.6",
    "express": "^4.22.2",
    "express-rate-limit": "^7.5.1",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "node-ssh": "^13.1.0",
    "socket.io": "^4.8.3",
    "winston": "^3.19.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/compression": "^1.8.1",
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.25",
    "@types/jsonwebtoken": "^9.0.10",
    "prisma": "^6.19.0",
    "typescript": "5.8.3"
  }
}
