/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateThresholdInput } from '../../models/create-threshold-input';
import { Threshold } from '../../models/threshold';

export interface CreateThreshold$Params {
      body: CreateThresholdInput
}

export function createThreshold(http: HttpClient, rootUrl: string, params: CreateThreshold$Params, context?: HttpContext): Observable<StrictHttpResponse<Threshold>> {
  const rb = new RequestBuilder(rootUrl, createThreshold.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Threshold>;
    })
  );
}

createThreshold.PATH = '/monitoring/thresholds';
