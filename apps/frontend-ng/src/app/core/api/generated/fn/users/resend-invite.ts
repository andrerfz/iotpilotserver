/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ResendInviteResult } from '../../models/resend-invite-result';

export interface ResendInvite$Params {

/**
 * Device public ID
 */
  id: string;
}

export function resendInvite(http: HttpClient, rootUrl: string, params: ResendInvite$Params, context?: HttpContext): Observable<StrictHttpResponse<ResendInviteResult>> {
  const rb = new RequestBuilder(rootUrl, resendInvite.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ResendInviteResult>;
    })
  );
}

resendInvite.PATH = '/users/{id}/resend-invite';
