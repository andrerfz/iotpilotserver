/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ActingTenant } from '../../models/acting-tenant';

export interface AuthActAsDelete$Params {
}

export function authActAsDelete(http: HttpClient, rootUrl: string, params?: AuthActAsDelete$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'success': boolean;
'data': ActingTenant;
'timestamp': string;
}>> {
  const rb = new RequestBuilder(rootUrl, authActAsDelete.PATH, 'delete');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'success': boolean;
      'data': ActingTenant;
      'timestamp': string;
      }>;
    })
  );
}

authActAsDelete.PATH = '/auth/act-as';
