/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TemperatureWebhookInput } from '../../models/temperature-webhook-input';
import { TemperatureWebhookResponse } from '../../models/temperature-webhook-response';

export interface IotTemperature$Params {
      body: TemperatureWebhookInput
}

export function iotTemperature(http: HttpClient, rootUrl: string, params: IotTemperature$Params, context?: HttpContext): Observable<StrictHttpResponse<TemperatureWebhookResponse>> {
  const rb = new RequestBuilder(rootUrl, iotTemperature.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<TemperatureWebhookResponse>;
    })
  );
}

iotTemperature.PATH = '/iot/temperature';
