/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface GetDeviceLogs$Params {
  id: string;
  level?: 'ALL' | 'DEBUG' | 'INFO' | 'WARN' | 'ERROR' | 'FATAL';
  source?: string;
  search?: string;
  limit?: number;
  offset?: number;
}

export function getDeviceLogs(http: HttpClient, rootUrl: string, params: GetDeviceLogs$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, getDeviceLogs.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('level', params.level, {});
    rb.query('source', params.source, {});
    rb.query('search', params.search, {});
    rb.query('limit', params.limit, {});
    rb.query('offset', params.offset, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

getDeviceLogs.PATH = '/devices/{id}/logs';
