/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface MonitoringReportsGet$Params {
  type?: 'alerts' | 'metrics' | 'thresholds' | 'system';
  period?: '1h' | '6h' | '24h' | '7d' | '30d';
  format?: 'json' | 'csv' | 'pdf' | 'html';
  deviceId?: string;
}

export function monitoringReportsGet(http: HttpClient, rootUrl: string, params?: MonitoringReportsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, monitoringReportsGet.PATH, 'get');
  if (params) {
    rb.query('type', params.type, {});
    rb.query('period', params.period, {});
    rb.query('format', params.format, {});
    rb.query('deviceId', params.deviceId, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

monitoringReportsGet.PATH = '/monitoring/reports';
