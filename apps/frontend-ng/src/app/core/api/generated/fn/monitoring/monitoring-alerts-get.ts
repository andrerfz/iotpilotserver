/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface MonitoringAlertsGet$Params {
  deviceId?: string;
  severity?: 'INFO' | 'WARNING' | 'ERROR' | 'CRITICAL';
  status?: 'active' | 'resolved' | 'acknowledged';
  startTime?: string;
  endTime?: string;
  limit?: number;
  offset?: number;
}

export function monitoringAlertsGet(http: HttpClient, rootUrl: string, params?: MonitoringAlertsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, monitoringAlertsGet.PATH, 'get');
  if (params) {
    rb.query('deviceId', params.deviceId, {});
    rb.query('severity', params.severity, {});
    rb.query('status', params.status, {});
    rb.query('startTime', params.startTime, {});
    rb.query('endTime', params.endTime, {});
    rb.query('limit', params.limit, {});
    rb.query('offset', params.offset, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

monitoringAlertsGet.PATH = '/monitoring/alerts';
