/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeviceSettings } from '../../models/device-settings';

export interface DevicesIdSettingsGet$Params {
  id: string;
}

export function devicesIdSettingsGet(http: HttpClient, rootUrl: string, params: DevicesIdSettingsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<DeviceSettings>> {
  const rb = new RequestBuilder(rootUrl, devicesIdSettingsGet.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeviceSettings>;
    })
  );
}

devicesIdSettingsGet.PATH = '/devices/{id}/settings';
