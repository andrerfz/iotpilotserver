/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NotificationSettings } from '../../models/notification-settings';
import { NotificationsSettingsInput } from '../../models/notifications-settings-input';

export interface UpdateNotificationSettings$Params {
      body: NotificationsSettingsInput
}

export function updateNotificationSettings(http: HttpClient, rootUrl: string, params: UpdateNotificationSettings$Params, context?: HttpContext): Observable<StrictHttpResponse<NotificationSettings>> {
  const rb = new RequestBuilder(rootUrl, updateNotificationSettings.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NotificationSettings>;
    })
  );
}

updateNotificationSettings.PATH = '/settings/notifications';
