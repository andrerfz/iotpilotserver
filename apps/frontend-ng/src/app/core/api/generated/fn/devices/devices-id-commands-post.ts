/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeviceCommand } from '../../models/device-command';

export interface DevicesIdCommandsPost$Params {
  id: string;
      body: {
'command': 'REBOOT' | 'SHUTDOWN' | 'UPDATE' | 'RESTART' | 'CUSTOM';
'arguments'?: string;
}
}

export function devicesIdCommandsPost(http: HttpClient, rootUrl: string, params: DevicesIdCommandsPost$Params, context?: HttpContext): Observable<StrictHttpResponse<DeviceCommand>> {
  const rb = new RequestBuilder(rootUrl, devicesIdCommandsPost.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<DeviceCommand>;
    })
  );
}

devicesIdCommandsPost.PATH = '/devices/{id}/commands';
