/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NotificationSettings } from '../../models/notification-settings';

export interface GetNotificationSettings$Params {
}

export function getNotificationSettings(http: HttpClient, rootUrl: string, params?: GetNotificationSettings$Params, context?: HttpContext): Observable<StrictHttpResponse<NotificationSettings>> {
  const rb = new RequestBuilder(rootUrl, getNotificationSettings.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NotificationSettings>;
    })
  );
}

getNotificationSettings.PATH = '/settings/notifications';
