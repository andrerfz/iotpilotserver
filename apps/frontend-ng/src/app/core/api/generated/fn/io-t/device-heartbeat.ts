/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HeartbeatInput } from '../../models/heartbeat-input';
import { HeartbeatResponse } from '../../models/heartbeat-response';

export interface DeviceHeartbeat$Params {
      body: HeartbeatInput
}

export function deviceHeartbeat(http: HttpClient, rootUrl: string, params: DeviceHeartbeat$Params, context?: HttpContext): Observable<StrictHttpResponse<HeartbeatResponse>> {
  const rb = new RequestBuilder(rootUrl, deviceHeartbeat.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HeartbeatResponse>;
    })
  );
}

deviceHeartbeat.PATH = '/iot/heartbeat';
