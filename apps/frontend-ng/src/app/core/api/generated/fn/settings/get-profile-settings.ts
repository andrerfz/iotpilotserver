/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProfileSettingsResponse } from '../../models/profile-settings-response';

export interface GetProfileSettings$Params {
}

export function getProfileSettings(http: HttpClient, rootUrl: string, params?: GetProfileSettings$Params, context?: HttpContext): Observable<StrictHttpResponse<ProfileSettingsResponse>> {
  const rb = new RequestBuilder(rootUrl, getProfileSettings.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProfileSettingsResponse>;
    })
  );
}

getProfileSettings.PATH = '/settings/profile';
