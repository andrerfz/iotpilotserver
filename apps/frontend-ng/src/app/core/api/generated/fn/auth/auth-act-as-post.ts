/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ActAsInput } from '../../models/act-as-input';
import { ActingTenant } from '../../models/acting-tenant';

export interface AuthActAsPost$Params {
      body: ActAsInput
}

export function authActAsPost(http: HttpClient, rootUrl: string, params: AuthActAsPost$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'success': boolean;
'data': ActingTenant;
'timestamp': string;
}>> {
  const rb = new RequestBuilder(rootUrl, authActAsPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'success': boolean;
      'data': ActingTenant;
      'timestamp': string;
      }>;
    })
  );
}

authActAsPost.PATH = '/auth/act-as';
