/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SecuritySettings } from '../../models/security-settings';
import { SecuritySettingsInput } from '../../models/security-settings-input';

export interface UpdateSecuritySettings$Params {
      body: SecuritySettingsInput
}

export function updateSecuritySettings(http: HttpClient, rootUrl: string, params: UpdateSecuritySettings$Params, context?: HttpContext): Observable<StrictHttpResponse<SecuritySettings>> {
  const rb = new RequestBuilder(rootUrl, updateSecuritySettings.PATH, 'put');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SecuritySettings>;
    })
  );
}

updateSecuritySettings.PATH = '/settings/security';
