/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ActivateDeviceResponse } from '../../models/activate-device-response';
import { IotRegisterInput } from '../../models/iot-register-input';

export interface IotRegisterDevice$Params {
      body: IotRegisterInput
}

export function iotRegisterDevice(http: HttpClient, rootUrl: string, params: IotRegisterDevice$Params, context?: HttpContext): Observable<StrictHttpResponse<ActivateDeviceResponse>> {
  const rb = new RequestBuilder(rootUrl, iotRegisterDevice.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ActivateDeviceResponse>;
    })
  );
}

iotRegisterDevice.PATH = '/iot/register';
