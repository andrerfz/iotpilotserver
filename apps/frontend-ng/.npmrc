# @angular/build declares vitest@^3 as a peerOptional, but this app pins vitest@4
# (via @analogjs/vitest-angular). That mismatch is benign and intentional; mirror
# the repo-root .npmrc so `npm i` inside this package doesn't ERESOLVE-fail.
legacy-peer-deps=true
