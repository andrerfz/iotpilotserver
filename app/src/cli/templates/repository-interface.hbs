import { {{pascalName}} } from '../entities/{{kebabName}}.entity';
import { {{pascalName}}Id } from '../value-objects/{{kebabName}}-id.vo';

/**
 * Repository interface for {{name}} entities
 * Defines the contract for data access operations
 *
 * @author {{data.author}}
 * @license {{data.license}}
 */
export interface {{pascalName}}Repository {
  /**
   * Find {{name}} by ID
   */
  findById(id: {{pascalName}}Id): Promise<{{pascalName}} | null>;

  /**
   * Find all {{name}} entities
   */
  findAll(): Promise<{{pascalName}}[]>;

  /**
   * Find {{name}} by name
   */
  findByName(name: string): Promise<{{pascalName}} | null>;

  /**
   * Save {{name}} entity
   */
  save({{camelName}}: {{pascalName}}): Promise<void>;

  /**
   * Check if {{name}} exists by ID
   */
  exists(id: {{pascalName}}Id): Promise<boolean>;

  /**
   * Get total count of {{name}} entities
   */
  count(): Promise<number>;

  /**
   * Soft delete {{name}} entity
   */
  delete(id: {{pascalName}}Id): Promise<void>;
}
