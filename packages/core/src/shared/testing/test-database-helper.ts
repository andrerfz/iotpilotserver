/**
 * Minimal TestDatabaseHelper for tenant-isolation-security tests.
 * Database operations are mocked in those tests; this is a no-op placeholder.
 */
export class TestDatabaseHelper {
  // Placeholder for tests that only need the helper to exist
}
